<resources>

    <!-- Base application theme. -->
    <style name="Ambi" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Color scheme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <!-- Default text style -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Main activity theme. -->
    <style name="Ambi.main" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Color scheme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <!-- Default text style -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Settings theme. -->
    <style name="Ambi.settings" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Settings text style -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>

        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="Ambi.widget" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Default text style -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/ambi_grey_5</item>
    </style>

    <!-- Header 3 -->
    <style name="H1">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Header 3 -->
    <style name="H3">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
